# learn-japanese

Голосовой агент для изучения японского: тренажёр чтения каны/кандзи на
скорость с мгновенным распознаванием речи и проактивный разговорный
собеседник на базе OpenAI Realtime, с повторением по кривой забывания (FSRS).

Статус: **спайк №2 закрыт с результатом go**. Web Speech API отклонён
([разбор](docs/SPIKE-ASR.md)); распознавание делает Vosk в браузере, причём
декодер ограничивается **одной ожидаемой морой** — вопрос сводится к «это она
или нет». Задержка 371–475 мс, распознавание 75–90 %, ложных срабатываний не
обнаружено ([разбор](docs/SPIKE-ASR-VOSK.md)). Дальше — MVP drill-режима
с FSRS. Продуктового кода пока нет.

## Документы

- [docs/VISION.md](docs/VISION.md) — описание идеи: режимы, механика,
  система повторений, порядок реализации.
- [docs/TECH.md](docs/TECH.md) — анализ вариантов стека и распознавания
  речи, рекомендации, первые прототипы-спайки.
- [docs/SPIKE-ASR.md](docs/SPIKE-ASR.md) — спайк №1 (Web Speech API):
  что измеряем, критерий go/no-go, результат no-go и его разбор.
- [docs/SPIKE-ASR-VOSK.md](docs/SPIKE-ASR-VOSK.md) — спайк №2 (Vosk
  с ограниченным словарём): устройство, что уже проверено, протокол замера.

## Быстрый старт

```bash
bun install
bun run fetch:model   # модель Vosk, 48 МБ, один раз
bun run dev
```

Открыть http://localhost:5173 в Chrome и произнести показанный символ.
Без микрофона — мок-режим с вводом ромадзи: http://localhost:5173/?mock=1
Для телефона нужен https: `bun run dev:https`, подробности в
[docs/SPIKE-ASR.md](docs/SPIKE-ASR.md).

Тесты и типы:

```bash
bun test && bun run typecheck
```

## Как работать с репозиторием

Проект разрабатывается в двух окружениях:

- **Облако (Claude Code on the web)** — проектирование, скелет кода, логика,
  тесты, документы. Изменения приходят ветками/PR в этот репозиторий.
- **Локально** — всё, что требует микрофона, звука и реальных устройств
  (drill-режим, Realtime-разговор). Клонировать в рабочую директорию:

```bash
git clone git@github.com:sergeiavoroshilov/learn-japanese.git ~/Development/learn-japanese
cd ~/Development/learn-japanese
```

Рантайм — [Bun](https://bun.sh): `curl -fsSL https://bun.sh/install | bash`.
